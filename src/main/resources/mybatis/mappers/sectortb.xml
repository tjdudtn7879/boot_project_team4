<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.dao.SectortbDAO">
	
	<select id="Sectortblist" resultType="com.boot.dto.SectortbDTO">
		SELECT * FROM sectortb
		order by deptno
	</select>

</mapper>
