<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.dao.PusertbDAO">
    <insert id="registerInsert">
        insert into pusertb (puserid, ppass, pname, email, phone)
        	values(#{puserid},#{ppass},#{pname},#{email},#{phone})
    </insert> 
</mapper>