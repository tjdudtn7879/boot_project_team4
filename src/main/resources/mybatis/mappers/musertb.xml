<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.dao.MusertbDAO">
    <select id="mloginYn" resultType="com.boot.dto.MusertbDTO">
    	select mpass, mname
    	from musertb
    	where muserid = #{muserid}
    </select>
</mapper>